rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All writes come from the backend service account only.
    // No client can read or write directly.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
